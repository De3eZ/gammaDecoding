import javax.swing.*;
public class Server extends Thread{
    public static void main(String[] args) throws InterruptedException {
        JFrame frame = new send("json send");
        frame.setVisible(true);
    }
}